apiVersion: v1
kind: Service
metadata:
  name: {{ include "fork-join-chart.fullname" . }}
  labels:
    app: {{ include "fork-join-chart.name" . }}
    helm.sh/chart: {{ include "fork-join-chart.chart" . }}
    release: {{ .Release.Name }}   
spec:
  type: {{ .Values.fork-join-angular.service.type }}
  ports:
    - port: {{ .Values.fork-join-angular.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "fork-join-chart.name" . }}
    helm.sh/chart: {{ include "fork-join-chart.chart" . }}
    release: {{ .Release.Name }}   
